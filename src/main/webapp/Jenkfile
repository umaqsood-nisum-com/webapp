//Declarative//
pipeline {
	agent any
	envirment {
		MAVEN_HOME = '/root/maven/appache-maven-3.5.3'
	}
	stages {
		stage(‘Build’) {
			steps {
				echo “Building”
				sh “${”MAVEN_HOME}/bin/mvn clean package”
			}
		}
	
	}
}
